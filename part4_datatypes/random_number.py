import random #imports the module for generating random number

print(random.randrange(1, 10)) #Generates a random number between 1 to 9(not 10)
#                               syntax:
#                                       random.randrange(startingNumber,endNumber-1)
